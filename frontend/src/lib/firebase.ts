// Firebase stub — replaced by Express + MongoDB REST API
// This file is kept for gradual migration. Do not add new code here.

export const db = {} as never;
export const auth = {
  currentUser: null as null | { uid: string; email: string | null },
} as never;
export const storage = null as never;
export const app = null as never;
